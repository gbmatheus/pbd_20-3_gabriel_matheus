package com.gabrielmatheus.eniatusapi.domain.services.descontos;


public class CadastrarDescontosService {

  // @Autowired
  // private DescontosRepository descontosRepository;

  // @Autowired
  // private FuncionarioRepository funcionarioRepository;

  // public Descontos createDescontos (Long id, Descontos descontos) {
  //   Funcionario funcionario =  funcionarioRepository.findById(id)
  //   .orElseThrow(() -> new BusinessException("Funcionário não existe"));

  //   descontos.setContribuicaoSindical(descontos.getContribuicaoSindical() == null ? new BigDecimal(0) : descontos.getContribuicaoSindical());
  //   descontos.setFaltas(descontos.getFaltas() == null ? 0 : descontos.getFaltas());
  //   descontos.setFuncionario(funcionario);
  //   descontos.setDataDesconto(LocalDateTime.now());

  //   // return descontosRepository.save(descontos);
  //   return null;
  // }
  
}
